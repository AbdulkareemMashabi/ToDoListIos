import Foundation
import SharedModels

enum AppConstants {
    static let appGroupIdentifier = WidgetAction.suiteName
    static let tokenKeychainKey = "token"
    static let favoriteTaskDefaultsKey = "favoriteTask"
    static let appLanguageDefaultsKey = "appLanguage"
    static let dateFormat = "dd/MM/yyyy"
}
